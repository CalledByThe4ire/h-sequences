/* eslint-disable import/prefer-default-export */
import { l, isEmpty, head, tail, concat, toString as listToString } from 'hexlet-pairs-data'; // eslint-disable-line
import { is, toString as htmlToString, hasChildren, children, reduce } from 'hexlet-html-tags'; // eslint-disable-line

// BEGIN (write your solution here)

// END
